

	let sum = 0;
	let length = 0;
	let average = 0;
	for(let count = 1 ; count <= 100 ; count++){
		sum = sum + count;
		length = length + 1;
		}
		average = sum / length;
		
		
		console.log("the average is: " + average);
	

